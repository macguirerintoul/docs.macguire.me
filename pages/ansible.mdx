# Ansible

## Install Ansible

### macOS

```zsh
brew install ansible
```

### Linux

```zsh
python3 -m pip install --user ansible
```

## Run a playbook

```zsh
ansible-playbook $PLAYBOOK_FILE -K
```
